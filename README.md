# Password-Manager
Password Manager which stores the data locally as JSON file.

Also has random password generator which generates a password between 12-18 character long and copied to clipboard too

Requires Tkinter, JSON, random and pyperclip to run
